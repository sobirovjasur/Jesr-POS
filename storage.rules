rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Any signed-in user can read and write storage objects (e.g. product
    // and profile images). Tighten per-path before production if needed.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
